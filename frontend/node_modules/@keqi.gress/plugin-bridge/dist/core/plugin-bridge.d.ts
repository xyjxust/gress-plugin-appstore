/**
 * 插件桥实现 - 主应用暴露给插件的 API
 *
 * 设计原则：
 * 1. 最小权限：默认只暴露必要的 API
 * 2. 安全隔离：通过代理控制访问
 * 3. 版本兼容：保持 API 稳定性
 */
import type { App } from 'vue';
import type { Router } from 'vue-router';
import type { Pinia } from 'pinia';
import type { PluginBridge } from '../types/plugin-system';
import { PluginPermission } from '../types/plugin-system';
/**
 * 创建插件桥实例
 */
export declare function createPluginBridge(options: {
    app: App;
    router: Router;
    pinia: Pinia;
    http: any;
    permissions?: PluginPermission[];
}): PluginBridge;
/**
 * 在全局对象上暴露插件桥
 */
export declare function exposePluginBridge(bridge: PluginBridge): void;
//# sourceMappingURL=plugin-bridge.d.ts.map